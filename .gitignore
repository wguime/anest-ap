# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
*.local

# Playwright MCP artifacts (snapshots, console logs)
.playwright-mcp/

# Playwright e2e artifacts (test runs, reports, traces)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Lighthouse audit artifacts (HTML/JSON reports — kept locally, summary em docs/)
lighthouse-*.report.html
lighthouse-*.report.json

# Environment & Firebase
.env
.env.*
!.env.example
.firebase/
.firebaserc

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Debug screenshots no root (sessões de Claude/Playwright)
/*.png

# Claude Code internals (handoffs, screenshots, worktrees)
.claude/handoff-*.md
.claude/auth-autonomous-mode.md
.claude/*.png
.claude/worktrees/

# macOS Finder duplicates (Cmd+D ou iCloud conflict)
* 2.*
* 2


# Codificação Anestésica — fontes/artefatos locais
Tabela Unimed/
e2e/__screenshots__/

# Corpus local de escalas (LGPD: imagens com nomes de pacientes — NUNCA commitar)
.local/
